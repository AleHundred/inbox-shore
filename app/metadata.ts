import type { Metadata } from 'next';

/**
 * Metadata for the application.
 */
export const metadata: Metadata = {
  title: 'Inbox Shore',
  description: 'Inbox Shore - A support interface for managing customer conversations',
};
