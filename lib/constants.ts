/**
 * Number of timeline entries to fetch per page
 */
export const TIMELINE_PAGE_SIZE = 20;

/**
 * Polling interval for timeline updates (in milliseconds)
 */
export const POLLING_INTERVAL = 5000;

/**
 * Maximum attempts to try loading more messages before determining we've reached the beginning
 */
export const MAX_LOAD_ATTEMPTS = 2;

/**
 * Scroll threshold for loading more messages (in pixels)
 */
export const SCROLL_THRESHOLD = 120;

/**
 * Title for the support application, displayed in the navigation bar
 */
export const SUPPORT_APP_TITLE = '🌊 Inbox Shore';
